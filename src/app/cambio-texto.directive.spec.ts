import { CambioTextoDirective } from './cambio-texto.directive';

describe('CambioTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioTextoDirective();
    expect(directive).toBeTruthy();
  });
});
